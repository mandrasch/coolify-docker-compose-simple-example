services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app

  db:
    image: mysql
    environment:
      MYSQL_DATABASE: db
      MYSQL_USER: db
      MYSQL_PASSWORD: db
      MYSQL_ROOT_PASSWORD: root

volumes:
  db_data: